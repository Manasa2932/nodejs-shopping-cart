apiVersion: v1
kind: Service
metadata:
  name: {{ include "shoppingcart.fullname" . }}
  labels:
    {{- include "shoppingcart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shoppingcart.selectorLabels" . | nindent 4 }}
